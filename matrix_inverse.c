#include <stdlib.h>
#include "datatypes.h"

Matrix* MTXinverse(Matrix* mtxA){
  if(!IsInvertable(mtxA)){
    break; //Ha az mtxA mátrix nem invertálható (determinánsa 0), akkor nincs inverze.
  }else{

  }
}
